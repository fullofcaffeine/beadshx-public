# Upstream oracle controls

This tree records exact source and tool locks plus scripts used to build and
observe the pinned Beads oracle. The oracle remains explicitly named and cannot
be a release command fallback.
