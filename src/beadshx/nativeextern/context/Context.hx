package beadshx.nativeextern.context;

/** Exact Go context interface used by exported Beads query roles. */
@:go.import("context")
@:go.package("context")
@:go.name("Context")
extern interface Context {}
